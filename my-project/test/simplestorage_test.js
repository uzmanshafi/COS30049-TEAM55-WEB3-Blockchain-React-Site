const SimpleStorage = artifacts.require("SimpleStorage");

contract("SimpleStorage", (accounts) => {
    it("should set and retrieve user data correctly", async () => {
        const instance = await SimpleStorage.deployed();
    });
    });